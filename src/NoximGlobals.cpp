// Stub for noxim global variables needed by Router.cpp
// Main.cpp (which defines main()) is not compiled — we use our own sc_main.
#include <cstddef>

// Referenced by Router.cpp via: extern unsigned int drained_volume;
unsigned int drained_volume = 0;